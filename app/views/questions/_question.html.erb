<tr>
    <td><%= question.body %></td>
    <td><%= link_to t('actions.show'), question_path(question) %></td>
    <td><%= link_to t('actions.edit'), edit_question_path(question) %></td>
    <td><%= link_to t('actions.delete'), question_path(question), 
        method: :delete, 
        data: { confirm: 'Are you sure?' } %></td>
</tr>



