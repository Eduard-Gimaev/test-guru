<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('.header') %></span>

  <div class="nav-admin user">
    <b class="nav-admin user-email"><%= t('actions.welcome', name: current_user.email ) %> | </b>
    <%= link_to t('actions.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>