<tr>
    <td><strong><%= answer.body  %></strong></td>
    <td><%= button_to t('actions.show'), admin_answer_path(answer), method: :get %></td>
    <td><%= button_to t('actions.edit'), edit_admin_answer_path(answer), method: :get %></td>
    <td><%= button_to t('actions.delete'), admin_answer_path(answer),
                method: :delete,
                data: { confirm: t('actions.confirm') } %></td>
</tr>