<tr>
    <td><strong><%= answer.body  %></strong></td>
    <td><%= link_to t('actions.show'), admin_answer_path(answer) %></td>
    <td><%= link_to t('actions.edit'), edit_admin_answer_path(answer) %></td>
    <td><%= link_to t('actions.delete'), admin_answer_path(answer),
                method: :delete,
                data: { confirm: t('actions.confirm') } %></td>
</tr>