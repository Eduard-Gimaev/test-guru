<tr>
    <td><%= question.body %></td>
    <td> <%= button_to t('actions.show'), admin_question_path(question), method: :get %> </td>
    <td> <%= button_to t('actions.edit'), edit_admin_question_path(question), method: :get %> </td>
    <td> <%= button_to t('actions.delete'), admin_question_path(question), 
                method: :delete, 
                data: { confirm: 'actions.confirm' } %></td>
</tr>



