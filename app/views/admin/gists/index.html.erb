<h2>Gists:</h2>
<%= link_to t('actions.back'), admin_tests_path %>
<table class="table table-hover">
  <thead class="table-dark">
    <tr style="text-align:center">
      <th ><%= t('.question') %></th>
      <th ><%= t('.url') %></th>
      <th ><%= t('.author') %></th>
      <th><%= t('.actions') %></th>

    </tr>
  </thead>
  <tbody>
    <%= render @gists %>
  </tbody>
</table>