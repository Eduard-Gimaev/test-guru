
  <tr>
    <td> <strong><%= test.title %></strong> </br>
      <span style="font-size:12px">
        <%=test_level(test)%> | <%=test.questions.count%>
      </span>
    </td>
    <td> <%= button_to t('actions.show'), admin_test_path(test), method: :get %> </td>
    <td> <%= button_to t('actions.edit'), edit_admin_test_path(test), method: :get %> </td>
    <td> <%= button_to t('actions.delete'), admin_test_path(test), 
                                            method: :delete, 
                                            data: { confirm: 'actions.confirm' } %></td>
  </tr>
  