<div class="row justify-content-center">
    <p>
      [ <%= link_to t('.new_badge'), new_admin_badge_path %> ]
      [ <%= link_to t('.users_badges'), admin_user_badges_path %> ]
      <%= link_to t('actions.back'), admin_tests_path %>
    </p>

  <div class="col col-md-4">
    <table class="table table-hover">
      <tr style="text-align:center">
        <th colspan="4" ><%= t('.header')%></th>
      </tr>
      <tr style="text-align:center" >
        <th class="w-50"><%= t('.title') %></th>
        <th colspan="3" ><%= t('actions.actions') %></th>
      </tr>
      <%= render @badges %>
    </table>
  </div>       
</div>