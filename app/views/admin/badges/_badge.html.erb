<tr>
    <td><%= badge.title %></td>
    <td> <%= button_to t('actions.show'), admin_badge_path(badge), method: :get %> </td>
    <td> <%= button_to t('actions.edit'), edit_admin_badge_path(badge), method: :get %> </td>
    <td> <%= button_to t('actions.delete'), admin_badge_path(badge), 
                method: :delete, 
                data: { confirm: 'actions.confirm' } %></td>
</tr>



