 <% if resource.present? && resource.errors.any? %>
    <div>
      <h2><%= pluralize(resourсe.errors.count, 'error') %> detected:</h2>
      <ul>
        <% resourсe.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  